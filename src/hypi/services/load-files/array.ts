

export default class ArrayLoadService implements LoadFileInterface {
    load(file: string, gl_type: string, mapping: string | undefined) {
        console.log('Load array');
        return "";
    }
}